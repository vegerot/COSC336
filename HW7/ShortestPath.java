public class ShortestPath
{
	ShortestPath(Graph graph,int start, int target)
	{
		int numNodesLeft= /*(find number of nodes and put here)*/;
		Stack s= new Stack;
		s.push(v);
		numNodesLeft --;
		while(!v.visited && numNodesLeft>0){
			v.visted=true;
			count ++;
			if(v =target){
				return count;	
			}
			ArrayList<Node> children =v.getChildren()
			for(int i=0;i<children.Size();i++){
				s.push(children<i>);
			}
			s.pop(v)
			
		}

	}
}
